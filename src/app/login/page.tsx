import { url } from '../layout';
import LogInContent from './LogInContent';

export const metadata = {
  title: 'Cop Slots Casino UK – Secure Login Steps and Player Access to Follow',
  alternates: {
    canonical: `https://${url}/login`,
  },
  description:
    'Cop Slots Casino UK provides a safe login procedure for every player. Learn how to register, verify, and sign in quickly to start your account journey today safely.',
};

export default function GamesPage() {
  return <LogInContent />;
}
